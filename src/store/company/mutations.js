export function name (state, payload) {
  // muta la empresa
  state.name = payload
}
export function type (state, payload) {
  // muta la empresa
  state.type = payload
}
export function active (state, payload) {
  // muta la empresa
  state.is_active = payload
}
